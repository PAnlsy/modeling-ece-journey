# Day 2 学习复盘

今天完成：
- 学习 str、int、float、bool 四种变量类型
- 使用 input() 获取用户输入
- 使用 if、elif、else 判断成绩等级
- 编写成绩等级判断器 day02_grade_checker.py

我理解了：
- input() 得到的是文字，需要用 float() 转成小数。
- if 会根据条件选择不同的代码分支。
- 程序可以根据输入产生不同结果。

明天要学：
- for 循环
- while 循环
- 列表的基本使用